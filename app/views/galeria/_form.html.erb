<%= form_for(@galerium) do |f| %>
  <% if @galerium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@galerium.errors.count, "erro") %> </h2>

      <ul>
      <% @galerium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.date_select :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
